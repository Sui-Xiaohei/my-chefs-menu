# My Chef’s Menu 第一版技术设计方案

Version: 0.6

## 1. 方案概述

第一版采用纯前端网页方案。用户填写的菜单内容只在当前浏览器页面中处理，不上传到服务器，也不保存到数据库。

第一版优先完成一条最短、可以实际使用的流程：

用户打开网页 → 在左侧输入菜单内容 → 点击 Generate → 在右侧查看菜单预览 → 在右侧切换菜单模板 → 下载 PNG

页面职责明确分开：左侧只负责输入菜单内容和发起 Generate；右侧负责展示已生成的结果、切换视觉风格和下载最终图片。模板选择不参与第一次生成流程。

为了让第一次开发网页的用户更容易理解，初始阶段不进行复杂的模块拆分。HTML、CSS 和 JavaScript 各自集中在少量文件中，先确保核心流程能够运行，再根据实际需要逐步拆分。

## 2. 第一版使用的技术

### 2.1 网页基础技术

- HTML：负责网页结构，包括左侧输入区和 Generate 按钮，以及右侧预览区、模板按钮和下载按钮。
- CSS：负责网页布局、基础外观和简单模板样式。
- JavaScript：负责在用户点击 Generate 后读取输入并生成预览，以及切换模板和触发 PNG 下载。

第一版使用原生 HTML、CSS 和 JavaScript，不使用 React、Vue 等框架。

### 2.2 本地开发工具

- Vite：启动本地网页、自动刷新页面，并生成用于发布的文件。
- npm：安装和管理 Vite 与图片下载工具。

### 2.3 PNG 下载

- html-to-image：把菜单预览区域转换成 PNG 图片。

第一版只使用一个图片转换工具。如果实际测试发现字体或导出效果不稳定，再评估是否更换为 html2canvas，不同时引入两套工具。

第一版只导出 DOM 元素 `#menu-preview-card`。输入区、Generate、Menu Style 控件、模板按钮、下载按钮和页面其他区域必须放在该元素之外，避免被写入图片。

### 2.4 暂缓使用的工具

第一阶段暂不强制加入 ESLint、Prettier、自动测试框架或其他工程化配置。等核心网页可以运行后，再判断是否需要添加，避免第一次开发时同时学习过多工具。

### 2.5 发布方式

未来可以使用 Vercel 或 Cloudflare Pages 发布静态网页。第一版不需要购买或维护服务器。

## 3. 为什么选择这个方案

### 3.1 符合当前 MVP 目标

输入、点击 Generate 生成预览、模板切换和 PNG 下载都可以直接在浏览器中完成，不需要账号、数据库或服务器。

### 3.2 对零基础用户更友好

初始代码只有四个主要文件。页面结构、样式和功能之间的关系更直观，遇到问题时也更容易判断应该查看哪个文件。

### 3.3 先完成可运行结果

第一阶段优先让网页能够打开并完成核心流程，不提前为未来功能创建空文件或复杂结构。这样每一步都能在浏览器中看到明确结果。

### 3.4 成本和隐私负担较低

用户输入只在当前页面中处理，不上传服务器，减少了数据库、安全维护和运行费用。

### 3.5 仍然可以逐步扩展

文件少并不代表以后不能扩展。当 `main.js` 或 `style.css` 明显变长、难以理解时，可以再按功能拆分。未来也可以增加更多模板、手机适配、测试工具和 AI 翻译服务。

## 4. 初始文件结构

```text
chef-menu-website/
├─ package.json       项目工具清单和启动命令
├─ index.html         网页入口和页面结构
└─ src/
   ├─ main.js         输入读取、Generate、模板切换和 PNG 下载功能
   └─ style.css       页面布局和菜单模板样式
```

现有的产品和开发文档继续保留，不属于网页运行代码：

```text
project-context.md
product-requirements.md
technical-design.md
development-log.md
```

### 4.1 `package.json`

记录项目名称、Vite、html-to-image，以及启动和生成发布文件所需的命令。

### 4.2 `index.html`

包含左侧菜单输入框和 Generate 按钮，以及右侧菜单预览、模板选择按钮和下载按钮，并加载 `main.js` 与 `style.css`。

### 4.3 `src/main.js`

第一版集中负责：

- 读取 Menu Title、Starter、Main Course、Dessert 和 Drink；
- 用户点击 Generate 后生成菜单预览；
- 预览生成后，保存用户在右侧选择的模板并切换对应样式；
- 将预览区域生成并下载为 PNG；
- 显示必要的下载状态或错误提示。

### 4.4 `src/style.css`

第一版集中负责：

- 网页的基础颜色和字体；
- 左侧输入区域与右侧结果区域的排列；
- 菜单卡片的尺寸、间距和留白；
- 初始模板和一个简单可切换模板的样式；
- 基础的手机屏幕显示规则。

## 5. 第一版开发步骤

### 第一步：让网页可以打开

创建四个初始文件，安装最少依赖，并用 Vite 启动本地网页。

完成标准：浏览器可以打开页面；左侧显示输入区和 Generate 按钮，右侧显示预览占位区、模板按钮和下载按钮。

### 第二步：输入菜单内容

加入 Menu Title、Starter、Main Course、Dessert 和 Drink 五个输入项。

完成标准：用户可以输入、修改和清空内容。

### 第三步：点击 Generate 生成菜单预览

在 `main.js` 中连接 Generate 按钮、五个输入项和预览区域。只有用户点击 Generate 时，程序才读取当前输入并生成菜单预览。

完成标准：输入或修改内容时预览不会自动变化；点击 Generate 后，预览一次性显示最新菜单内容；空内容不会导致页面无法使用。

### 第四步：切换简单模板

在右侧预览区域加入模板选择功能。先制作基础模板和一个简单的可切换样式，用最少代码验证模板切换方法。模板按钮不参与第一次生成；用户生成预览后再进行视觉调整。

完成标准：生成预览后可以在右侧切换模板；切换时菜单内容保持不变，只改变背景、文字、间距等视觉样式。

完整的 Classic Fine Dining、Modern 和 Romantic Dinner 三套精细模板作为后续视觉完善任务，不阻挡第一条核心流程完成。

### 第五步：下载 PNG

使用 html-to-image 把 `#menu-preview-card` 转换成 PNG，并触发浏览器下载。第一版采用单一的 `4:5` 竖版导出比例，不提供尺寸选择器，输出尺寸统一为 `1080 × 1350 px`。

完成标准：下载图片只包含菜单卡片，与预览基本一致，文字清楚，中文和英文不乱码；下载失败时有容易理解的提示；电脑和移动端都能完成下载。

### 第六步：基础检查

人工检查空输入、中文、英文、较长菜名、模板切换和重复下载。

完成标准：用户能够从打开网页开始，连续完成左侧输入、点击 Generate、查看右侧预览、切换模板和下载，没有阻断操作的问题。

## 6. 核心功能的简单实现方式

### 6.1 菜单输入和数据

`index.html` 提供五个输入框和 Generate 按钮。用户可以先自由填写和修改内容；点击 Generate 后，`main.js` 读取五个输入框，并把当时的内容保存在一个简单的菜单对象中：

```text
菜单数据
├─ title：菜单标题
├─ starter：前菜
├─ mainCourse：主菜
├─ dessert：甜点
└─ drink：饮品
```

数据只保存在当前打开的页面中。刷新或关闭网页后内容消失，这符合第一版不提供在线保存的范围。

### 6.2 Generate 生成预览

用户输入时不更新预览。点击 Generate 后，`main.js` 读取全部菜单数据，并一次性更新对应的预览文字。用户内容以普通文字显示，不作为网页指令执行。

Generate 使用默认模板创建第一次预览，不读取任何预先选择的模板。用户修改左侧输入后，右侧已生成的结果保持不变，直到用户再次点击 Generate。

输入框可以显示浅色示例，帮助新用户理解需要填写什么；生成预览和图片时不应显示未填写字段的提示文字。

这种方式让操作更接近一次菜单创作过程，而不是持续变化的实时编辑工具。Live Preview 可以在未来版本中作为可选增强功能加入。

### 6.3 简单模板切换

模板选择控件位于右侧结果区域，并在菜单预览生成后供用户使用。用户选择模板时，`main.js` 只更换预览区域的 CSS 类名，`style.css` 根据类名改变颜色、字体、间距和装饰。

切换模板不会重新读取左侧输入，也不会改变已生成的菜单文字。左侧负责内容，右侧负责结果和视觉调整，两边职责保持分离。

这样可以学习模板切换的基本原理，同时避免复制菜单内容或提前创建多个模块。

### 6.4 PNG 下载

#### 导出范围

点击下载按钮后，`main.js` 等待字体加载，只调用 html-to-image 读取 `#menu-preview-card`，然后生成固定清晰度的 PNG。

`#menu-preview-card` 是唯一的导出边界。以下内容不得放入该元素：

- 左侧输入区域和 Generate 按钮；
- Menu Style 标题和模板选择按钮；
- Download 按钮和下载状态提示；
- 页面标题、欢迎语及其他布局元素。

#### 下载按钮交互

- 菜单尚未生成时，下载按钮隐藏或使用低干扰的禁用状态。
- 菜单生成后，下载按钮才进入可操作状态。
- 按钮采用弱提示视觉层级，不与主要的 Generate 操作竞争注意力。
- 下载过程中按钮显示“Generating...”并暂时禁用，防止重复点击。
- 下载成功或失败后恢复按钮；失败时显示简单的重试提示。
- 移动端按钮应位于右侧结果区域对应的预览和视觉调整控件附近，使用足够大的触控区域；窄屏上下排列时不应超出屏幕。

#### 图片比例与 Export Style

第一版只实现一种统一的 `4:5` 竖版比例，技术实现和测试以 `1080 × 1350 px` 为固定输出尺寸，不增加比例选择控件。

尺寸含义：

```text
宽度：1080 px
高度：1350 px
比例：4:5
```

未来可把比例和尺寸封装为多个 Export Style，扩展其他比例，例如：

- Social Portrait：社交媒体竖版；
- Story：手机全屏故事比例；
- Landscape：横版比例；
- Print：适合打印的纸张比例。

未来切换 Export Style 时，只调整导出画布尺寸和菜单排版，不改变用户输入的菜单内容。该能力不属于第一版 MVP。

需要重点检查：

- 导出区域严格为 `#menu-preview-card`，不包含输入框和任何控制按钮；
- 中文和英文能够正确显示；
- 较长文字可以换行；
- 下载过程中按钮显示“正在生成”；
- 生成失败时提示用户重试。

## 7. 第一版暂时不创建的内容

初始阶段不创建：

- `translation-service.js`；
- 菜单状态、预览、模板和导出的独立模块文件；
- 多个 CSS 分类文件；
- 自动测试目录和复杂构建配置；
- 账号、数据库、服务器或 AI 接口。

暂时不创建这些内容，是为了减少第一次开发需要同时理解的文件和概念，不代表永久取消。

## 8. 未来扩展方向

当第一版核心流程稳定后，可以按实际需要逐步扩展：

1. 把三个正式菜单模板完善为 Classic Fine Dining、Modern 和 Romantic Dinner。
2. 加强手机和不同电脑屏幕的适配。
3. 增加字体、装饰图片和更精细的导出效果。
4. 当 `main.js` 过长时，再拆分菜单数据、预览、模板选择和图片导出模块。
5. 当 `style.css` 过长时，再拆分基础样式、页面布局和模板样式。
6. 增加人工或自动测试工具。
7. 在未来版本中增加 AI 翻译和菜单文案优化。
8. 根据用户反馈考虑增加 Live Preview，让输入内容可选择立即同步到预览。
9. 增加多个 Export Style，为社交媒体、Story 和打印场景提供不同图片比例。

未来加入 AI 时，应通过项目自己的服务器调用 AI 服务。AI 密钥不能写进浏览器代码。AI 不可用时，手动输入、预览和下载仍应正常工作。

## 9. 简化版 MVP 完成标准

简化版第一阶段完成，需要满足：

- 网页可以在本地浏览器中打开；
- 五个菜单字段可以填写，并在点击 Generate 后生成预览；
- 左侧只负责输入内容和点击 Generate，输入变化不会实时同步到右侧；
- 右侧负责展示生成结果，并可在基础样式和一个简单模板之间切换；
- 模板选择不参与第一次生成，切换模板时菜单内容不丢失；
- `#menu-preview-card` 可以按第一版 `4:5` 比例成功下载为 `1080 × 1350 px` PNG，图片不包含输入区、Menu Style 控件或页面其他元素；
- 中文和英文可以正确显示；
- 不要求登录，不连接数据库，不调用 AI；
- 用户内容默认不上传服务器。

完成以上流程后，再进入三套模板精细设计、完整响应式适配和发布阶段。
